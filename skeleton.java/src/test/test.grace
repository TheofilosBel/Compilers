fun p1 (a ,b : int) : nothing
    
    var ab, ac : int[5][6][1][7];
    
    fun p2 (c, d, dd : int ) : int;
    
    var donut, muffin : char ;
    
    {
        x[4] <- 3;
        $$
        f(x+5, 3);
        
        x <- (a-5) + 3;
        
        if not TRUE then 
        	muffin <- "MUFFIN\n";
        
        if a > -5 or a < 4 then
            a <- 3 * b div 5;
        else
            a <- 9;

        while x > 3 do x <- x + 1;
        $$
    }